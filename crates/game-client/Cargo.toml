[package]
name = "game-client"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode = "1.3.3"
bevy = "0.18.1"
bevy_renet = { version = "4.0.1", features = ["netcode"] }
shared = { path = "../shared" }
uuid = { version = "1.18.1", features = ["serde", "v4"] }
